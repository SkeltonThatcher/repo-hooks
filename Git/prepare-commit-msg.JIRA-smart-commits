#!/bin/bash +x

# Starts with e.g. "JRA-1234 "
PATTERN="^[A-Z]{3}-[\d]{1,6}\s"

MATCHES=$(grep -E $PATTERN "$1")

echo Matches: "$MATCHES"

if ("$MATCHES") then
	exit 0
else
	echo "Commit message does not match $PATTERN"
	exit 1
fi
